import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: [
      'utfs.io',
      'img.clerk.com',
      'subdomain',
      'assets.aceternity.com',
      'images.unsplash.com'

    ],
  },
  reactStrictMode: false,
};

export default nextConfig;
